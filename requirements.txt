# LangGraph 多Agent 核心
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0   # OpenAI兼容接口，用于DeepSeek/通义千问等国产模型

# Web服务
fastapi>=0.110.0
uvicorn[standard]>=0.24.0

# 数据层
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# RAG检索（BM25为默认，零额外依赖；如需向量检索取消下面注释）
jieba>=0.42.1             # 中文分词，用于BM25
# faiss-cpu>=1.7.4        # 向量检索（可选升级）
# sentence-transformers   # Embedding模型（可选升级）

# 模板与测试
jinja2>=3.1.0
pytest>=7.4.0
httpx>=0.25.0
