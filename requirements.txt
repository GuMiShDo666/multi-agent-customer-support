# LangGraph 多Agent 核心（固定为同一兼容版本组，保证可复现安装）
langgraph==0.2.76
langchain-core==0.3.40
langchain-openai==0.3.7   # OpenAI兼容接口，用于DeepSeek/通义千问等国产模型
langgraph-checkpoint==2.1.2
langgraph-sdk==0.1.74
langsmith==0.3.45
openai==1.109.1

# Web服务
fastapi==0.115.8
uvicorn[standard]==0.34.0

# 数据层
sqlalchemy==2.0.38
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1

# RAG检索（BM25为默认，零额外依赖；如需向量检索取消下面注释）
jieba==0.42.1             # 中文分词，用于BM25
# faiss-cpu>=1.7.4        # 向量检索（可选升级）
# sentence-transformers   # Embedding模型（可选升级）

# 模板与测试
jinja2==3.1.5
pytest==8.3.4
httpx==0.28.1
